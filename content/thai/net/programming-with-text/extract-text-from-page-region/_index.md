---
title: แยกข้อความจากพื้นที่หน้าในไฟล์ PDF
linktitle: แยกข้อความจากพื้นที่หน้าในไฟล์ PDF
second_title: เอกสารอ้างอิง Aspose.PDF สำหรับ API ของ .NET
description: เรียนรู้วิธีแยกข้อความจากพื้นที่เฉพาะบนหน้าในไฟล์ PDF โดยใช้ Aspose.PDF สำหรับ .NET
type: docs
weight: 190
url: /th/net/programming-with-text/extract-text-from-page-region/
---
บทช่วยสอนนี้จะแนะนำคุณตลอดกระบวนการแยกข้อความจากพื้นที่เฉพาะบนหน้าในไฟล์ PDF โดยใช้ Aspose.PDF สำหรับ .NET โค้ดต้นฉบับ C# ที่ให้มาจะสาธิตขั้นตอนที่จำเป็น

## ความต้องการ
ก่อนที่คุณจะเริ่มต้น ให้แน่ใจว่าคุณมีสิ่งต่อไปนี้:

- Visual Studio หรือคอมไพเลอร์ C# อื่น ๆ ติดตั้งอยู่บนเครื่องของคุณ
- Aspose.PDF สำหรับไลบรารี .NET คุณสามารถดาวน์โหลดได้จากเว็บไซต์ Aspose อย่างเป็นทางการหรือใช้ตัวจัดการแพ็คเกจเช่น NuGet เพื่อติดตั้ง

## ขั้นตอนที่ 1: ตั้งค่าโครงการ
1. สร้างโครงการ C# ใหม่ในสภาพแวดล้อมการพัฒนาที่คุณต้องการ
2. เพิ่มการอ้างอิงถึงไลบรารี Aspose.PDF สำหรับ .NET

## ขั้นตอนที่ 2: นำเข้าเนมสเปซที่จำเป็น
ในไฟล์โค้ดที่คุณต้องการแยกข้อความ ให้เพิ่มคำสั่ง using ต่อไปนี้ที่ด้านบนของไฟล์:

```csharp
using Aspose.Pdf;
using System.IO;
```

## ขั้นตอนที่ 3: ตั้งค่าไดเรกทอรีเอกสาร
 ในโค้ด ให้ค้นหาบรรทัดที่ระบุว่า`string dataDir = "YOUR DOCUMENT DIRECTORY";` และแทนที่`"YOUR DOCUMENT DIRECTORY"` พร้อมเส้นทางไปยังไดเร็กทอรีที่คุณเก็บเอกสารไว้

## ขั้นตอนที่ 4: เปิดเอกสาร PDF
 เปิดเอกสาร PDF ที่มีอยู่โดยใช้`Document` ตัวสร้างและส่งผ่านเส้นทางไปยังไฟล์ PDF อินพุต

```csharp
Document pdfDocument = new Document(dataDir + "ExtractTextAll.pdf");
```

## ขั้นตอนที่ 5: แยกข้อความจากพื้นที่หน้า
 สร้าง`TextAbsorber` วัตถุที่จะดึงข้อความจากเอกสาร กำหนดค่า`TextSearchOptions` เพื่อจำกัดการค้นหาให้อยู่ในบริเวณหน้าเฉพาะที่กำหนดโดยสี่เหลี่ยมผืนผ้า

```csharp
TextAbsorber absorb = new TextAbsorber();
absorb.TextSearchOptions.LimitToPageBounds = true;
absorb.TextSearchOptions.Rectangle = new Aspose.Pdf.Rectangle(100, 200, 250, 350);
pdfDocument.Pages[1].Accept(absorb);
```

## ขั้นตอนที่ 6: รับข้อความที่แยกออกมา
 เข้าถึงข้อความที่แยกออกมาจาก`TextAbsorber` วัตถุ.

```csharp
string extractedText = absorb.Text;
```

## ขั้นตอนที่ 7: บันทึกข้อความที่แยกออกมา
 สร้าง`TextWriter` และเปิดไฟล์ที่คุณต้องการบันทึกข้อความที่แยกออกมา เขียนข้อความที่แยกออกมาลงในไฟล์และปิดสตรีม

```csharp
TextWriter tw = new StreamWriter(dataDir + "extracted-text.txt");
tw.WriteLine(extractedText);
tw. Close();
```

### ตัวอย่างโค้ดต้นฉบับสำหรับการดึงข้อความจากพื้นที่หน้าโดยใช้ Aspose.PDF สำหรับ .NET 
```csharp
// เส้นทางไปยังไดเร็กทอรีเอกสาร
string dataDir = "YOUR DOCUMENT DIRECTORY";
// เปิดเอกสาร
Document pdfDocument = new Document(dataDir + "ExtractTextAll.pdf");
// สร้างวัตถุ TextAbsorber เพื่อแยกข้อความ
TextAbsorber absorber = new TextAbsorber();
absorber.TextSearchOptions.LimitToPageBounds = true;
absorber.TextSearchOptions.Rectangle = new Aspose.Pdf.Rectangle(100, 200, 250, 350);
// รับตัวดูดซับสำหรับหน้าแรก
pdfDocument.Pages[1].Accept(absorber);
// รับข้อความที่แยกออกมา
string extractedText = absorber.Text;
// สร้างโปรแกรมเขียนและเปิดไฟล์
TextWriter tw = new StreamWriter(dataDir + "extracted-text.txt");
// เขียนบรรทัดข้อความลงในไฟล์
tw.WriteLine(extractedText);
// ปิดลำธาร
tw.Close();
```

## บทสรุป
คุณได้แยกข้อความจากพื้นที่เฉพาะบนหน้าเอกสาร PDF สำเร็จแล้วโดยใช้ Aspose.PDF สำหรับ .NET ข้อความที่แยกออกมาได้รับการบันทึกลงในไฟล์เอาต์พุตที่ระบุ

### คำถามที่พบบ่อย

#### ถาม: จุดประสงค์ของบทช่วยสอนนี้คืออะไร?

A: บทช่วยสอนนี้มีวัตถุประสงค์เพื่อแนะนำคุณตลอดกระบวนการแยกข้อความจากพื้นที่เฉพาะบนหน้าในไฟล์ PDF โดยใช้ Aspose.PDF สำหรับ .NET โค้ดต้นฉบับ C# ที่แนบมามีคำแนะนำทีละขั้นตอนสำหรับการทำงานนี้ให้สำเร็จ

#### ถาม: ฉันควรนำเข้าเนมสเปซอะไรบ้าง?

ก: ในไฟล์โค้ดที่คุณต้องการแยกข้อความ ให้ใส่คำสั่ง using ต่อไปนี้ไว้ที่จุดเริ่มต้นของไฟล์:

```csharp
using Aspose.Pdf;
using System.IO;
```

#### ถาม: ฉันจะระบุไดเรกทอรีเอกสารได้อย่างไร?

 ก. ค้นหาเส้น`string dataDir = "YOUR DOCUMENT DIRECTORY";` ในโค้ดและแทนที่`"YOUR DOCUMENT DIRECTORY"` พร้อมเส้นทางจริงไปยังไดเร็กทอรีเอกสารของคุณ

#### ถาม: ฉันจะเปิดเอกสาร PDF ที่มีอยู่ได้อย่างไร

 ก: ในขั้นตอนที่ 4 คุณจะเปิดเอกสาร PDF ที่มีอยู่โดยใช้`Document` ตัวสร้างและกำหนดเส้นทางไปยังไฟล์ PDF อินพุต

#### ถาม: ฉันจะแยกข้อความจากบริเวณหน้าเฉพาะได้อย่างไร

 A: ขั้นตอนที่ 5 เกี่ยวข้องกับการสร้าง`TextAbsorber`วัตถุที่จะแยกข้อความจากเอกสาร PDF จากนั้นคุณจะกำหนดค่า`TextSearchOptions` เพื่อกำหนดพื้นที่สี่เหลี่ยมที่เจาะจงบนหน้าโดยใช้พิกัด

#### ถาม: ฉันจะเข้าถึงข้อความที่แยกออกมาได้อย่างไร

 ก: ขั้นตอนที่ 6 จะแนะนำคุณเกี่ยวกับการเข้าถึงข้อความที่แยกออกมาจาก`TextAbsorber` วัตถุ.

#### ถาม: ฉันจะบันทึกข้อความที่แยกออกมาลงในไฟล์ได้อย่างไร

 A: ในขั้นตอนที่ 7 คุณจะสร้าง`TextWriter`เปิดไฟล์ที่คุณต้องการบันทึกข้อความที่แยกออกมา เขียนข้อความที่แยกออกมาลงในไฟล์ แล้วจึงปิดสตรีม

#### ถาม: สิ่งสำคัญที่ได้จากบทช่วยสอนนี้คืออะไร?

A: เมื่อทำตามบทช่วยสอนนี้ คุณจะได้เรียนรู้วิธีแยกข้อความจากพื้นที่เฉพาะบนหน้าเอกสาร PDF โดยใช้ Aspose.PDF สำหรับ .NET ข้อความที่แยกออกมาจะถูกบันทึกลงในไฟล์เอาต์พุตที่ระบุ ช่วยให้คุณสามารถกำหนดเป้าหมายและวิเคราะห์เนื้อหาข้อความที่ต้องการได้อย่างแม่นยำ