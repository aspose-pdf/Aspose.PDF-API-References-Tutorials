---
title: Укажите межстрочный интервал в PDF-файле
linktitle: Укажите межстрочный интервал в PDF-файле
second_title: Справочник по API Aspose.PDF для .NET
description: Узнайте, как указать межстрочный интервал в PDF-файле с помощью Aspose.PDF для .NET.
type: docs
weight: 510
url: /ru/net/programming-with-text/specify-line-spacing/
---
В этом руководстве объясняется, как задать межстрочный интервал в файле PDF с помощью Aspose.PDF для .NET. Предоставленный исходный код C# демонстрирует процесс шаг за шагом.

## Предпосылки

Прежде чем приступить к изучению руководства, убедитесь, что у вас есть следующее:

- Базовые знания языка программирования C#.
- Установлена библиотека Aspose.PDF для .NET. Вы можете получить ее с сайта Aspose или использовать NuGet для установки в свой проект.

## Шаг 1: Настройте проект

Начните с создания нового проекта C# в предпочитаемой вами интегрированной среде разработки (IDE) и добавьте ссылку на библиотеку Aspose.PDF для .NET.

## Шаг 2: Импортируйте необходимые пространства имен

Добавьте следующие директивы using в начало файла C#, чтобы импортировать необходимые пространства имен:

```csharp
using Aspose.Pdf;
using Aspose.Pdf.Text;
using System.IO;
```

## Шаг 3: Укажите путь к каталогу документов.

 Задайте путь к каталогу ваших документов с помощью`dataDir` переменная:

```csharp
string dataDir = "YOUR DOCUMENT DIRECTORY";
```

 Заменять`"YOUR DOCUMENT DIRECTORY"` с фактическим путем к каталогу ваших документов.

## Шаг 4: Загрузите входной PDF-файл.

 Загрузите входной PDF-файл с помощью`Document` сорт:

```csharp
Document doc = new Document();
```

## Шаг 5: Создание параметров форматирования текста

 Создать`TextFormattingOptions` объект и установите режим межстрочного интервала`FullSize`:

```csharp
TextFormattingOptions formattingOptions = new TextFormattingOptions();
formattingOptions.LineSpacing = TextFormattingOptions.LineSpacingMode.FullSize;
```

## Шаг 6: Создание текстового фрагмента

 Создать`TextFragment` объект и укажите текстовое содержимое:

```csharp
TextFragment textFragment = new TextFragment("Hello world");
```

## Шаг 7: Загрузите файл шрифта (необязательно)

 Если вы хотите использовать определенный шрифт для текста, загрузите файл шрифта TrueType в`FileStream` объект:

```csharp
string fontFile = dataDir + "HPSimplified.TTF";
using (FileStream fontStream = File.OpenRead(fontFile))
{
    textFragment.TextState.Font = FontRepository.OpenFont(fontStream, FontTypes.TTF);
}
```

 Заменять`"HPSimplified.TTF"` с фактическим именем файла шрифта.

## Шаг 8: Укажите положение текста и межстрочный интервал.

 Установите позицию для фрагмента текста и назначьте`TextFormattingOptions` к`TextState.FormattingOptions` свойство:

```csharp
textFragment.Position = new Position(100, 600);
textFragment.TextState.FormattingOptions = formattingOptions;
```

## Шаг 9: Добавьте текст в документ

 Добавьте фрагмент текста в документ, либо присоединив его к`TextBuilder` или непосредственно на страницу`Paragraphs` коллекция:

```csharp
var page = doc.Pages.Add();
page.Paragraphs.Add(textFragment);
```

## Шаг 10: Сохраните полученный PDF-документ.

Сохраните измененный PDF-документ:

```csharp
dataDir = dataDir + "SpecifyLineSpacing_out.pdf";
doc.Save(dataDir);
```

 Обязательно замените`"SpecifyLineSpacing_out.pdf"` с желаемым именем выходного файла.

### Пример исходного кода для указания межстрочного интервала с помощью Aspose.PDF для .NET 
```csharp
// Путь к каталогу документов.
string dataDir = "YOUR DOCUMENT DIRECTORY";
string fontFile = dataDir + "HPSimplified.TTF";
// Загрузить входной PDF-файл
Document doc = new Document();
//Создайте параметры форматирования текста с помощью LineSpacingMode.FullSize
TextFormattingOptions formattingOptions = new TextFormattingOptions();
formattingOptions.LineSpacing = TextFormattingOptions.LineSpacingMode.FullSize;
// Создать объект текстового конструктора для первой страницы документа
//TextBuilder textBuilder = new TextBuilder(doc.Pages[1]);
// Создать фрагмент текста с образцом строки
TextFragment textFragment = new TextFragment("Hello world");
if (fontFile != "")
{
	// Загрузить шрифт TrueType в потоковый объект
	using (FileStream fontStream = System.IO.File.OpenRead(fontFile))
	{
		// Задайте имя шрифта для текстовой строки
		textFragment.TextState.Font = FontRepository.OpenFont(fontStream, FontTypes.TTF);
		//Укажите позицию для текстового фрагмента
		textFragment.Position = new Position(100, 600);
		//Установите TextFormattingOptions текущего фрагмента на предопределенное значение (что указывает на LineSpacingMode.FullSize)
		textFragment.TextState.FormattingOptions = formattingOptions;
		// Добавьте текст в TextBuilder, чтобы его можно было разместить поверх PDF-файла.
		//textBuilder.AppendText(textFragment);
		var page = doc.Pages.Add();
		page.Paragraphs.Add(textFragment);
	}
	dataDir = dataDir + "SpecifyLineSpacing_out.pdf";
	// Сохранить полученный PDF-документ
	doc.Save(dataDir);
}
```

## Заключение

Поздравляем! Вы успешно научились указывать межстрочный интервал в документе PDF с помощью Aspose.PDF для .NET. В этом руководстве представлено пошаговое руководство от настройки проекта до сохранения измененного документа. Теперь вы можете включить этот код в свои собственные проекты C# для настройки межстрочного интервала текста в файлах PDF.

### Часто задаваемые вопросы

#### В: Какова цель урока «Указание межстрочного интервала в файле PDF»?

A: Учебник "Указание межстрочного интервала в PDF-файле" призван помочь пользователям использовать библиотеку Aspose.PDF для .NET для настройки межстрочного интервала текста в PDF-документе. Учебник содержит пошаговые инструкции и примеры кода C# для демонстрации процесса.

#### В: Как это руководство поможет вам задать межстрочный интервал в документе PDF?

A: Этот учебник помогает пользователям понять, как использовать возможности Aspose.PDF для .NET для указания межстрочного интервала для текста в документе PDF. Следуя предоставленным шагам и примерам кода, пользователи могут настроить межстрочный интервал в соответствии со своими предпочтениями.

#### В: Какие предварительные условия необходимы для прохождения этого урока?

A: Перед началом обучения у вас должно быть базовое понимание языка программирования C#. Кроме того, у вас должна быть установлена библиотека Aspose.PDF for .NET. Вы можете получить ее на веб-сайте Aspose или установить в своем проекте с помощью NuGet.

#### В: Как мне настроить свой проект, следуя этому руководству?

A: Чтобы начать, создайте новый проект C# в предпочитаемой вами интегрированной среде разработки (IDE) и добавьте ссылку на библиотеку Aspose.PDF для .NET. Это позволит вам использовать возможности библиотеки для работы с PDF-документами и настройки межстрочного интервала.

#### В: Могу ли я использовать это руководство для указания межстрочного интервала для любого типа текста?

A: Да, в этом руководстве приведены инструкции по указанию межстрочного интервала для любого текстового содержимого в документе PDF с использованием Aspose.PDF для .NET. Вы можете использовать предоставленные примеры кода для настройки межстрочного интервала текста в соответствии с вашими потребностями.

#### В: Как указать режим межстрочного интервала в руководстве?

 A: В руководстве показано, как создать`TextFormattingOptions` объект и установите его`LineSpacing` собственность`TextFormattingOptions.LineSpacingMode.FullSize`. Этот режим задает полный межстрочный интервал для текстового содержимого.

#### В: Как загрузить определенный шрифт для текста?

 A: Если вы хотите использовать определенный шрифт для текстового содержимого, в руководстве приведены инструкции по загрузке файла шрифта TrueType в`FileStream` объект и установите его как шрифт для`TextFragment`. Это позволяет вам настраивать шрифт текста, а также межстрочный интервал.

#### В: Как настроить положение текста в PDF-документе?

 A: Чтобы настроить положение текста, создайте`TextFragment` объект и установите его`Position`свойство к нужным координатам (X и Y). Это позволяет контролировать, где текст будет размещен в документе PDF.

#### В: Могу ли я применить эти изменения межстрочного интервала к существующим PDF-документам?

 A: Да, вы можете изменить межстрочный интервал для текста в существующих документах PDF. В руководстве показано, как создать`TextFragment` с указанным межстрочным интервалом и позицией, а затем добавить его на страницу`Paragraphs` коллекция.