---
title: حدد الصفحة عند العرض
linktitle: حدد الصفحة عند العرض
second_title: مرجع واجهة برمجة التطبيقات Aspose.PDF لـ .NET
description: تعرف على كيفية تحديد صفحة عند عرض ملف PDF باستخدام Aspose.PDF لـ .NET.
type: docs
weight: 110
url: /ar/net/programming-with-links-and-actions/specify-page-when-viewing/
---
تعرف على كيفية تحديد صفحة عند عرض ملف PDF باستخدام Aspose.PDF لـ .NET باستخدام هذا الدليل خطوة بخطوة.

## الخطوة 1: إعداد البيئة

تأكد من إعداد بيئة التطوير الخاصة بك باستخدام مشروع C# والمراجع Aspose.PDF المناسبة.

## الخطوة 2: تحميل ملف PDF

قم بتعيين مسار الدليل الخاص بمستنداتك وقم بتحميل ملف PDF باستخدام الكود التالي:

```csharp
// المسار إلى دليل المستندات.
string dataDir = "YOUR DOCUMENTS DIRECTORY";
// تحميل ملف PDF
Document doc = new Document(dataDir + "SpecifyPageWhenViewing.pdf");
```

## الخطوة 3: تحديد الصفحة المستهدفة

احصل على نموذج الصفحة المستهدفة باستخدام الكود التالي:

```csharp
Page page2 = doc.Pages[2];
```

 يمكنك تعديل الفهرس`[2]` لتحديد الصفحة المطلوبة.

## الخطوة 4: تكوين إعداد التكبير

إنشاء متغير لتعيين عامل تكبير الصفحة المستهدفة:

```csharp
double zoom = 1;
```

يمكنك ضبط قيمة التكبير وفقًا لاحتياجاتك.

## الخطوة 5: إنشاء إجراء التنقل

إنشاء مثيل لإجراء التنقل باستخدام صفحة الهدف المحددة:

```csharp
GoToAction action = new GoToAction(doc.Pages[2]);
```

## الخطوة 6: تحديد الوجهة

قم بتعيين الوجهة للانتقال إلى الصفحة المستهدفة باستخدام الإحداثيات والتكبير:

```csharp
action.Destination = new XYZExplicitDestination(page2, 0, page2.Rect.Height, zoom);
```

## الخطوة 7: تكوين إجراء فتح المستند

قم بتعيين إجراء فتح المستند باستخدام إجراء التنقل الذي تم إنشاؤه:

```csharp
doc. OpenAction = action;
```

## الخطوة 8: احفظ المستند المحدث

 احفظ المستند المحدث باستخدام`Save` طريقة:

```csharp
doc.Save(dataDir + "goto2page_out.pdf");
```

### عينة من كود المصدر لتحديد الصفحة عند العرض باستخدام Aspose.PDF لـ .NET 
```csharp
// المسار إلى دليل المستندات.
string dataDir = "YOUR DOCUMENT DIRECTORY";
// تحميل ملف PDF
Document doc = new Document(dataDir + "SpecifyPageWhenViewing.pdf");
// احصل على نسخة من الصفحة الثانية من المستند
Page page2 = doc.Pages[2];
// إنشاء المتغير لتعيين عامل التكبير للصفحة المستهدفة
double zoom = 1;
// إنشاء مثيل GoToAction
GoToAction action = new GoToAction(doc.Pages[2]);
// انتقل إلى الصفحة 2
action.Destination = new XYZExplicitDestination(page2, 0, page2.Rect.Height, zoom);
// تعيين إجراء فتح المستند
doc.OpenAction = action;
// حفظ المستند المحدث
doc.Save(dataDir + "goto2page_out.pdf");
```

## خاتمة

تهانينا! أنت الآن تعرف كيفية تحديد صفحة عند عرض ملف PDF باستخدام Aspose.PDF لـ .NET. استخدم هذه المعرفة لتخصيص تجربة العرض للمستخدم في مستندات PDF الخاصة بك.

الآن بعد أن أكملت هذا الدليل، يمكنك تطبيق هذه المفاهيم على مشاريعك الخاصة واستكشاف الميزات التي يقدمها Aspose.PDF لـ .NET بشكل أكبر.

### الأسئلة الشائعة 

#### س: ما هو الغرض من تحديد صفحة الهدف عند عرض ملف PDF؟

ج: يتيح لك تحديد صفحة مستهدفة التحكم في الصفحة التي يتم عرضها من مستند PDF عند فتح الملف. ويمكن أن يؤدي هذا إلى تحسين تجربة المستخدم من خلال توجيهه إلى صفحة معينة تهمه.

#### س: كيف يمكن أن يكون تحديد صفحة الهدف مفيدًا في مستندات PDF؟

أ: يعد تحديد صفحة مستهدفة مفيدًا عندما تريد توجيه المستخدمين إلى قسم أو محتوى معين داخل مستند PDF دون الحاجة إلى التنقل يدويًا عبر الصفحات.

#### س: كيف يسهل Aspose.PDF لـ .NET تحديد صفحة مستهدفة للعرض؟

ج: يوفر Aspose.PDF لـ .NET واجهات برمجة التطبيقات التي تسمح لك بتعيين العرض الأولي لمستند PDF، بما في ذلك الصفحة المستهدفة، ومستوى التكبير، وخصائص العرض الأخرى.

#### س: هل يمكنني تحديد أي صفحة لتكون الصفحة المستهدفة؟

ج: نعم، يمكنك تحديد أي صفحة داخل مستند PDF كصفحة مستهدفة للعرض. ما عليك سوى استخدام الفهرس المناسب لتحديد الصفحة المطلوبة.

#### س: ما أهمية عامل التكبير عند تحديد صفحة الهدف؟

ج: يحدد عامل التكبير مستوى التكبير المطبق على الصفحة المستهدفة عند فتح مستند PDF. ويتحكم في مقدار المحتوى المعروض داخل نافذة العرض.

#### س: هل يمكنني تعيين عوامل تكبير مختلفة لصفحات مستهدفة مختلفة؟

ج: نعم، يمكنك تعيين عوامل تكبير مختلفة لصفحات مستهدفة مختلفة عن طريق إنشاء صفحات منفصلة`GoToAction` الحالات وتكوين وجهاتها وفقًا لذلك.

#### س: هل هناك أي قيود على تحديد الصفحة المستهدفة؟

ج: يقتصر تحديد صفحة مستهدفة على التحكم في العرض الأولي عند فتح ملف PDF. ولا يؤثر ذلك على تفاعلات المستخدم أو التنقل بمجرد عرض ملف PDF.

#### س: هل يمكنني استخدام هذه الميزة لإنشاء عروض تقديمية داخل مستند PDF؟

ج: نعم، يمكنك استخدام هذه الميزة لإنشاء تجارب تشبه العروض التقديمية داخل مستند PDF، وتوجيه المستخدمين عبر الأقسام أو الموضوعات المختلفة.

#### س: هل يمكنني تخصيص جوانب أخرى من العرض الأولي، مثل تخطيط الصفحة؟

ج: نعم، يوفر Aspose.PDF لـ .NET خصائص لتخصيص جوانب أخرى من العرض الأولي، بما في ذلك تخطيط الصفحة ووضع الصفحة والمزيد.

#### س: كيف يمكنني اختبار ما إذا كانت الصفحة المستهدفة وعامل التكبير المحددين يعملان كما هو مقصود؟

ج: بعد تطبيق الكود المقدم لتحديد الصفحة المستهدفة وعامل التكبير، افتح ملف PDF المعدل وتأكد من فتحه بالصفحة الصحيحة ومستوى التكبير.