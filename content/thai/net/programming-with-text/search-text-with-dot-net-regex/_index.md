---
title: ค้นหาข้อความด้วย Dot Net Regex
linktitle: ค้นหาข้อความด้วย Dot Net Regex
second_title: เอกสารอ้างอิง Aspose.PDF สำหรับ API ของ .NET
description: เรียนรู้วิธีค้นหาข้อความโดยใช้นิพจน์ทั่วไปของ .NET ในเอกสาร PDF โดยใช้ Aspose.PDF สำหรับ .NET
type: docs
weight: 480
url: /th/net/programming-with-text/search-text-with-dot-net-regex/
---
บทช่วยสอนนี้จะอธิบายวิธีการใช้ Aspose.PDF สำหรับ .NET เพื่อค้นหาข้อความโดยใช้นิพจน์ทั่วไปของ .NET ในเอกสาร PDF โค้ดต้นฉบับ C# ที่ให้มาจะสาธิตขั้นตอนต่างๆ ของกระบวนการนี้ทีละขั้นตอน

## ข้อกำหนดเบื้องต้น

ก่อนที่จะดำเนินการตามบทช่วยสอน โปรดแน่ใจว่าคุณมีสิ่งต่อไปนี้:

- ความรู้พื้นฐานเกี่ยวกับภาษาการเขียนโปรแกรม C#
- ติดตั้งไลบรารี Aspose.PDF สำหรับ .NET แล้ว คุณสามารถดาวน์โหลดได้จากเว็บไซต์ Aspose หรือใช้ NuGet เพื่อติดตั้งในโปรเจ็กต์ของคุณ

## ขั้นตอนที่ 1: ตั้งค่าโครงการ

เริ่มต้นด้วยการสร้างโครงการ C# ใหม่ในสภาพแวดล้อมการพัฒนาแบบบูรณาการ (IDE) ที่คุณต้องการ และเพิ่มการอ้างอิงไปยังไลบรารี Aspose.PDF สำหรับ .NET

## ขั้นตอนที่ 2: นำเข้าเนมสเปซที่จำเป็น

เพิ่มคำสั่ง using ต่อไปนี้ที่จุดเริ่มต้นของไฟล์ C# ของคุณเพื่อนำเข้าเนมสเปซที่จำเป็น:

```csharp
using Aspose.Pdf;
using Aspose.Pdf.Text;
```

## ขั้นตอนที่ 3: ตั้งค่าเส้นทางไปยังไดเรกทอรีเอกสาร

 ตั้งค่าเส้นทางไปยังไดเรกทอรีเอกสารของคุณโดยใช้`dataDir` ตัวแปร:

```csharp
string dataDir = "YOUR DOCUMENT DIRECTORY";
```

 แทนที่`"YOUR DOCUMENT DIRECTORY"` พร้อมเส้นทางจริงไปยังไดเร็กทอรีเอกสารของคุณ

## ขั้นตอนที่ 4: สร้างวัตถุ Regex .NET

 สร้าง`.NET Regex` วัตถุที่จะกำหนดรูปแบบการค้นหา:

```csharp
System.Text.RegularExpressions.Regex regex = new System.Text.RegularExpressions.Regex(@"[\S]+");
```

 แทนที่`@"[\S]+"` ด้วยรูปแบบนิพจน์ปกติของคุณตามที่ต้องการ

## ขั้นตอนที่ 5: โหลดเอกสาร PDF

 โหลดเอกสาร PDF โดยใช้`Document` ระดับ:

```csharp
Aspose.Pdf.Document document = new Aspose.Pdf.Document(dataDir + "SearchTextRegex.pdf");
```

 แทนที่`"SearchTextRegex.pdf"` ด้วยชื่อจริงของไฟล์ PDF ของคุณ

## ขั้นตอนที่ 6: รับหน้าเฉพาะ

รับหน้าเอกสารที่ต้องการ:

```csharp
Page page = document.Pages[1];
```

 แทนที่`1` พร้อมระบุหมายเลขหน้าที่ต้องการ (ดัชนีแบบ 1)

## ขั้นตอนที่ 7: สร้าง TextFragmentAbsorber

 สร้าง`TextFragmentAbsorber` วัตถุในการค้นหาอินสแตนซ์ทั้งหมดของนิพจน์ทั่วไปของอินพุต:

```csharp
TextFragmentAbsorber textFragmentAbsorber = new TextFragmentAbsorber(regex);
textFragmentAbsorber.TextSearchOptions.IsRegularExpressionUsed = true;
```

## ขั้นตอนที่ 8: ยอมรับตัวดูดซับสำหรับหน้า

ยอมรับตัวดูดซับสำหรับหน้า:

```csharp
page.Accept(textFragmentAbsorber);
```

## ขั้นตอนที่ 9: ดึงข้อมูลส่วนที่แยกออกมา

รับชิ้นส่วนข้อความที่แยกออกมาโดยใช้`TextFragments` ทรัพย์สินของ`TextFragmentAbsorber` วัตถุ:

```csharp
TextFragmentCollection textFragmentCollection = textFragmentAbsorber.TextFragments;
```

## ขั้นตอนที่ 10: วนซ้ำผ่านชิ้นส่วนข้อความ

วนซ้ำผ่านชิ้นส่วนข้อความที่เรียกค้นมาและดำเนินการตามต้องการ:

```csharp
foreach (TextFragment textFragment in textFragmentCollection)
{
	Console.WriteLine(textFragment.Text);
}
```

แก้ไขโค้ดภายในลูปเพื่อดำเนินการเพิ่มเติมกับส่วนข้อความแต่ละส่วนหากจำเป็น

### ตัวอย่างโค้ดต้นฉบับสำหรับการค้นหาข้อความด้วย Dot Net Regex โดยใช้ Aspose.PDF สำหรับ .NET 
```csharp
string dataDir = "YOUR DOCUMENT DIRECTORY";
// สร้างวัตถุ Regex เพื่อค้นหาคำทั้งหมด
System.Text.RegularExpressions.Regex regex = new System.Text.RegularExpressions.Regex(@"[\S]+");
// เปิดเอกสาร
Aspose.Pdf.Document document = new Aspose.Pdf.Document(dataDir + "SearchTextRegex.pdf");
// รับหน้าเฉพาะ
Page page = document.Pages[1];
// สร้างวัตถุ TextAbsorber เพื่อค้นหาอินสแตนซ์ทั้งหมดของ regex อินพุต
TextFragmentAbsorber textFragmentAbsorber = new TextFragmentAbsorber(regex);
textFragmentAbsorber.TextSearchOptions.IsRegularExpressionUsed = true;
// รับตัวดูดซับสำหรับหน้า
page.Accept(textFragmentAbsorber);
// รับชิ้นส่วนข้อความที่แยกออกมา
TextFragmentCollection textFragmentCollection = textFragmentAbsorber.TextFragments;
// วนผ่านชิ้นส่วน
foreach (TextFragment textFragment in textFragmentCollection)
{
	Console.WriteLine(textFragment.Text);
}
```

## บทสรุป

ขอแสดงความยินดี! คุณได้เรียนรู้วิธีค้นหาข้อความโดยใช้นิพจน์ทั่วไปของ .NET ในเอกสาร PDF โดยใช้ Aspose.PDF สำหรับ .NET สำเร็จแล้ว บทช่วยสอนนี้ให้คำแนะนำทีละขั้นตอนตั้งแต่การตั้งค่าโครงการไปจนถึงการเข้าถึงส่วนข้อความที่แยกออกมา ตอนนี้คุณสามารถนำโค้ดนี้ไปใช้กับโครงการ C# ของคุณเองเพื่อค้นหาข้อความขั้นสูงในไฟล์ PDF

### คำถามที่พบบ่อย

#### ถาม: จุดประสงค์ของบทช่วยสอน "ค้นหาข้อความด้วย Dot Net Regex" คืออะไร

A: บทช่วยสอน "ค้นหาข้อความด้วย Dot Net Regex" มีวัตถุประสงค์เพื่อแนะนำผู้ใช้ในการใช้ไลบรารี Aspose.PDF สำหรับ .NET เพื่อค้นหาข้อความภายในเอกสาร PDF โดยใช้นิพจน์ทั่วไปของ .NET บทช่วยสอนนี้ให้คำแนะนำทีละขั้นตอนและตัวอย่างโค้ด C# เพื่อสาธิตกระบวนการ

#### ถาม: บทช่วยสอนนี้ช่วยในการค้นหาข้อความโดยใช้นิพจน์ทั่วไปของ .NET ใน PDF ได้อย่างไร

A: บทช่วยสอนนี้จะช่วยให้ผู้ใช้เข้าใจถึงวิธีการใช้ประโยชน์จากความสามารถของ Aspose.PDF สำหรับ .NET เพื่อค้นหาข้อความโดยใช้นิพจน์ทั่วไปของ .NET ภายในเอกสาร PDF โดยทำตามขั้นตอนและตัวอย่างโค้ดที่ให้มา ผู้ใช้สามารถค้นหารูปแบบข้อความที่ตรงกับนิพจน์ทั่วไปที่ระบุได้อย่างมีประสิทธิภาพ

#### ถาม: มีข้อกำหนดเบื้องต้นอะไรบ้างที่ต้องปฏิบัติตามบทช่วยสอนนี้?

A: ก่อนเริ่มบทช่วยสอน คุณควรมีความเข้าใจพื้นฐานเกี่ยวกับภาษาการเขียนโปรแกรม C# นอกจากนี้ คุณต้องติดตั้งไลบรารี Aspose.PDF สำหรับ .NET คุณสามารถรับได้จากเว็บไซต์ Aspose หรือติดตั้งในโปรเจ็กต์ของคุณโดยใช้ NuGet

#### ถาม: ฉันจะตั้งค่าโครงการเพื่อทำตามบทช่วยสอนนี้ได้อย่างไร

ก: ในการเริ่มต้น ให้สร้างโปรเจ็กต์ C# ใหม่ในสภาพแวดล้อมการพัฒนาแบบบูรณาการ (IDE) ที่คุณต้องการ และเพิ่มการอ้างอิงไปยังไลบรารี Aspose.PDF สำหรับ .NET วิธีนี้จะช่วยให้คุณใช้คุณลักษณะของไลบรารีในการค้นหาและทำงานกับเอกสาร PDF ได้

#### ถาม: ฉันสามารถใช้บทช่วยสอนนี้เพื่อค้นหาข้อความประเภทใดประเภทหนึ่งโดยเฉพาะโดยใช้นิพจน์ทั่วไปของ .NET ได้หรือไม่

 A: ใช่ บทช่วยสอนนี้ให้คำแนะนำเกี่ยวกับวิธีการค้นหาข้อความโดยใช้นิพจน์ทั่วไปของ .NET ภายในเอกสาร PDF คุณสามารถปรับแต่งได้`.NET Regex` วัตถุเพื่อกำหนดรูปแบบการค้นหาเฉพาะที่คุณต้องการใช้

#### ถาม: ฉันจะระบุรูปแบบนิพจน์ทั่วไปของ .NET เพื่อค้นหาในบทช่วยสอนนี้ได้อย่างไร

 A: เพื่อระบุรูปแบบนิพจน์ทั่วไปของ .NET ที่คุณต้องการค้นหา ให้สร้าง`.NET Regex` วัตถุและกำหนดรูปแบบโดยใช้รูปแบบนิพจน์ทั่วไปที่เหมาะสม แทนที่ค่าเริ่มต้น`@"[\S]+"` ในโค้ดของบทช่วยสอนโดยใช้นิพจน์ปกติที่คุณต้องการ

#### ถาม: ฉันจะดึงคุณสมบัติของชิ้นส่วนข้อความที่แยกออกมาได้อย่างไร

 ก. หลังจากยอมรับ`TextFragmentAbsorber` สำหรับหน้าเฉพาะของ PDF คุณสามารถดึงข้อมูลส่วนที่แยกออกมาได้โดยใช้`TextFragments` คุณสมบัติของวัตถุตัวดูดซับ ซึ่งให้สิทธิ์ในการเข้าถึงคอลเล็กชันของส่วนข้อความที่ตรงกับนิพจน์ทั่วไปของ .NET ที่ระบุ

#### ถาม: ฉันสามารถปรับแต่งโค้ดเพื่อดำเนินการเพิ่มเติมกับข้อความแต่ละส่วนที่แยกออกมาได้หรือไม่

A: แน่นอน โค้ดตัวอย่างของบทช่วยสอนประกอบด้วยลูปสำหรับวนซ้ำผ่านข้อความย่อยที่ดึงออกมา คุณสามารถปรับแต่งโค้ดภายในลูปนี้เพื่อดำเนินการเพิ่มเติมกับข้อความย่อยที่ดึงออกมาแต่ละส่วนตามความต้องการของโครงการของคุณ

#### ถาม: ฉันจะบันทึกเอกสาร PDF ที่แก้ไขแล้วหลังจากการแยกส่วนข้อความได้อย่างไร

A: บทช่วยสอนนี้มุ่งเน้นไปที่การค้นหาข้อความโดยใช้นิพจน์ทั่วไปของ .NET และการดึงข้อมูลส่วนต่างๆ ของข้อความ หากคุณต้องการปรับเปลี่ยน PDF คุณสามารถดูเอกสาร Aspose.PDF อื่นๆ เพื่อเรียนรู้วิธีการจัดการและบันทึกเอกสารตามความต้องการเฉพาะของคุณ