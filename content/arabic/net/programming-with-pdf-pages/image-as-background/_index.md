---
title: تعيين الصورة كخلفية للصفحة في ملف PDF
linktitle: تعيين الصورة كخلفية للصفحة في ملف PDF
second_title: مرجع واجهة برمجة التطبيقات Aspose.PDF لـ .NET
description: دليل خطوة بخطوة لتعيين صورة كخلفية للصفحة في ملف PDF باستخدام Aspose.PDF لـ .NET.
type: docs
weight: 110
url: /ar/net/programming-with-pdf-pages/image-as-background/
---
في هذا البرنامج التعليمي، سنوضح لك عملية خطوة بخطوة لتعيين صورة كخلفية للصفحة باستخدام Aspose.PDF for .NET. وسنشرح لك الكود المصدري المرفق بلغة C# وسنقدم لك دليلاً شاملاً لمساعدتك على فهم هذه الميزة وتنفيذها في مشاريعك الخاصة. وفي نهاية هذا البرنامج التعليمي، ستعرف كيفية إضافة صورة كخلفية للصفحة في مستند PDF باستخدام Aspose.PDF for .NET.

## المتطلبات الأساسية
قبل أن تبدأ، تأكد من أن لديك ما يلي:

- المعرفة الأساسية بلغة البرمجة C#
- تم تثبيت Aspose.PDF لـ .NET في بيئة التطوير الخاصة بك

## الخطوة 1: تحديد دليل المستندات
أولاً، عليك تحديد المسار إلى دليل المستندات. هذا هو الموقع الذي تريد حفظ مستند PDF الذي قمت بتحريره فيه. استبدل "دليل المستندات الخاص بك" بالمسار المناسب.

```csharp
string dataDir = "YOUR DOCUMENTS DIRECTORY";
```

## الخطوة 2: إنشاء مستند جديد
 يمكنك بعد ذلك إنشاء كائن مستند جديد باستخدام`Document` فصل.

```csharp
Document doc = new Document();
```

## الخطوة 3: إضافة صفحة جديدة إلى المستند
 الآن يمكنك إضافة صفحة جديدة إلى كائن المستند باستخدام`Add()` طريقة`Pages` فصل.

```csharp
Page page = doc.Pages.Add();
```

## الخطوة 4: إنشاء كائن أثري للخلفية
يمكنك بعد ذلك إنشاء كائن BackgroundArtifact جديد لتعيين صورة الخلفية.

```csharp
BackgroundArtifact background = new BackgroundArtifact();
background.BackgroundImage = File.OpenRead(dataDir + "aspose-total-for-net.jpg");
```

## الخطوة 5: أضف الخلفية إلى الصفحة
يمكنك بعد ذلك إضافة كائن BackgroundArtifact إلى مجموعة القطع الأثرية للصفحة باستخدام`Artifacts` ممتلكات`Page` فصل.

```csharp
page. Artifacts. Add(background);
```

## الخطوة 6: احفظ مستند PDF
 أخيرًا، يمكنك حفظ مستند PDF في ملف باستخدام`Save()` طريقة`Document`تأكد من تحديد المسار الصحيح واسم الملف.

```csharp
doc.Save(dataDir + "ImageAsBackground_out.pdf");
```

### عينة من كود المصدر لـ Image As Background باستخدام Aspose.PDF لـ .NET 

```csharp

// المسار إلى دليل المستندات.
string dataDir = "YOUR DOCUMENT DIRECTORY";
// إنشاء كائن مستند جديد
Document doc = new Document();
// إضافة صفحة جديدة إلى كائن المستند
Page page = doc.Pages.Add();
// إنشاء كائن أثري في الخلفية
BackgroundArtifact background = new BackgroundArtifact();
// حدد الصورة لكائن الخلفية
background.BackgroundImage = File.OpenRead(dataDir + "aspose-total-for-net.jpg");
// إضافة قطعة أثرية خلفية إلى مجموعة القطع الأثرية للصفحة
page.Artifacts.Add(background);
dataDir = dataDir + "ImageAsBackground_out.pdf";
// حفظ المستند
doc.Save(dataDir);
System.Console.WriteLine("\nImage as page background added successfully.\nFile saved at " + dataDir);

```

## خاتمة
في هذا البرنامج التعليمي، تعلمنا كيفية تعيين صورة كخلفية لصفحة في مستند PDF باستخدام Aspose.PDF لـ .NET. باتباع هذا الدليل التفصيلي، يمكنك بسهولة إضافة صورة خلفية إلى مستندات PDF الخاصة بك. يوفر Aspose.PDF واجهة برمجة تطبيقات قوية ومرنة للعمل مع ملفات PDF، بما في ذلك تخصيص خلفية الصفحة. يمكنك الآن تطبيق هذه الميزة في مشاريعك الخاصة لإنشاء مستندات PDF بصور خلفية مخصصة

### الأسئلة الشائعة حول تعيين الصورة كخلفية للصفحة في ملف PDF

#### س: كيف يمكنني تعيين صورة كخلفية للصفحة في مستند PDF باستخدام Aspose.PDF لـ .NET؟

أ: لتعيين صورة كخلفية للصفحة في مستند PDF باستخدام Aspose.PDF لـ .NET، يمكنك اتباع الخطوات التالية:

1. قم بتعيين دليل المستند عن طريق تحديد المسار الذي تريد حفظ مستند PDF المحرر فيه.
2.  إنشاء كائن مستند جديد باستخدام`Document` فصل.
3.  أضف صفحة جديدة إلى كائن المستند باستخدام`Add()` طريقة`Pages` فصل.
4.  قم بإنشاء كائن BackgroundArtifact جديد لتعيين صورة الخلفية. يمكنك تحديد ملف الصورة باستخدام`File.OpenRead()` طريقة.
5.  أضف كائن BackgroundArtifact إلى مجموعة القطع الأثرية للصفحة باستخدام`Artifacts` ممتلكات`Page` فصل.
6.  احفظ مستند PDF في ملف باستخدام`Save()` طريقة`Document` الفئة، وتحديد المسار الصحيح واسم الملف للإخراج.

#### س: هل يمكنني إضافة صور خلفية متعددة إلى صفحات مختلفة من مستند PDF؟

ج: نعم، يمكنك إضافة صور خلفية متعددة إلى صفحات مختلفة من مستند PDF عن طريق تكرار العملية الموضحة في البرنامج التعليمي لكل صفحة. ما عليك سوى إنشاء كائن BackgroundArtifact جديد بالصورة المطلوبة لكل صفحة وإضافته إلى مجموعة الآثار الخاصة بالصفحة المعنية.

#### س: هل يمكنني تطبيق تغيير حجم الصورة أو وضعها على صورة الخلفية في الصفحة؟

 ج: نعم، يمكنك تطبيق تغيير حجم الصورة أو تحديد موضعها على صورة الخلفية في الصفحة من خلال التلاعب بها.`background.BackgroundImage` خاصية كائن BackgroundArtifact. قبل إضافة BackgroundArtifact إلى الصفحة، يمكنك تعديل خصائص الصورة، مثل العرض والارتفاع والموضع، لتخصيص كيفية ظهور الصورة كخلفية.

#### س: هل يدعم Aspose.PDF لـ .NET إضافة صور خلفية إلى مستندات PDF الموجودة مع المحتوى؟

ج: نعم، يتيح لك برنامج Aspose.PDF for .NET إضافة صور خلفية إلى مستندات PDF الموجودة التي تحتوي على محتوى. يمكنك تحميل مستند PDF موجود، وإضافة صورة الخلفية إلى الصفحة المطلوبة، ثم حفظ المستند المحدث في ملف جديد أو استبدال الملف الأصلي.

#### س: هل يمكنني استخدام صور بتنسيقات مختلفة كخلفية للصفحة، مثل PNG أو BMP؟

ج: نعم، يمكنك استخدام صور بتنسيقات مختلفة كخلفية للصفحة، مثل PNG أو BMP، بالإضافة إلى تنسيق JPEG المستخدم في البرنامج التعليمي. يدعم Aspose.PDF for .NET مجموعة كبيرة من تنسيقات الصور، ويمكنك استخدام أي تنسيق صورة مدعوم كخلفية لصفحات PDF.