---
title: Таблица в разделе «Заголовок и нижний колонтитул»
linktitle: Таблица в разделе «Заголовок и нижний колонтитул»
second_title: Справочник по API Aspose.PDF для .NET
description: Узнайте, как добавить таблицу в раздел верхнего/нижнего колонтитула PDF-документа с помощью Aspose.PDF для .NET.
type: docs
weight: 170
url: /ru/net/programming-with-stamps-and-watermarks/table-in-header-footer-section/
---
В этом уроке мы шаг за шагом покажем вам, как добавить таблицу в раздел верхнего или нижнего колонтитула документа PDF с помощью Aspose.PDF для .NET. Предоставленный исходный код C# показывает, как создать пустой документ PDF, добавить страницу, настроить раздел верхнего колонтитула, создать таблицу, добавить строки и ячейки в таблицу и, наконец, сохранить документ PDF.

## Шаг 1: Настройка среды

Прежде чем начать, убедитесь, что у вас есть следующее:

- Установленная среда разработки .NET.
- Библиотека Aspose.PDF для .NET, загруженная и указанная в вашем проекте.

## Шаг 2: Создание PDF-документа и страницы

 Первый шаг — создать экземпляр`Document` класс и добавить страницу в документ. Вот как:

```csharp
// Путь к каталогу документов.
string dataDir = "YOUR DOCUMENTS DIRECTORY";

// Создать экземпляр объекта Document
Aspose.Pdf.Document pdfDocument = new Aspose.Pdf.Document();

// Создать страницу в PDF-документе
Aspose.Pdf.Page page = pdfDocument.Pages.Add();
```

Обязательно замените «КАТАЛОГ ВАШИХ ДОКУМЕНТОВ» фактическим путем к каталогу, в котором вы хотите сохранить PDF-документ.

## Шаг 3: Настройка раздела заголовка

 Теперь мы настроим раздел заголовка PDF-документа, создав экземпляр`HeaderFooter` класс. Вот как:

```csharp
// Создайте раздел заголовка для PDF-файла
Aspose.Pdf.HeaderFooter header = new Aspose.Pdf.HeaderFooter();

// Определите раздел заголовка для страницы
page. Header = header;

// Установите верхнее поле раздела заголовка
header. Margin. Top = 20;
```

## Шаг 4: Создание таблицы

 Теперь мы создадим таблицу с помощью`Table` класс и добавьте его в коллекцию абзацев раздела заголовка. Вот как:

```csharp
// Создать экземпляр объекта Table
Aspose.Pdf.Table tab1 = new Aspose.Pdf.Table();

// Добавить таблицу в коллекцию абзацев раздела заголовка
header.Paragraphs.Add(tab1);

// Определите ширину столбцов таблицы.
tab1.ColumnWidths = "60,300";
```

Приведенный выше код создает таблицу с двумя столбцами указанной ширины.

## Шаг 5: Добавьте строки и ячейки в таблицу.

 Теперь мы добавим строки и ячейки в таблицу с помощью`Row` класс и`Cell` класс. Вот как:

```csharp
// Создайте строку в таблице и добавьте ячейки
Aspose.Pdf.Row row1 = tab1.Rows.Add();
row1.Cells.Add("Table in header section");
row1.BackgroundColor = Color.Gray;

// Объединить первую ячейку первой строки
tab1.Rows[0].Cells[0].ColSpan = 2;
tab1.Rows[0].Cells[0].DefaultCellTextState.ForegroundColor = Color.Cyan;
tab1.Rows[0].Cells[0].DefaultCellTextState.Font = FontRepository.FindFont("Helvetica");

// Создайте еще одну строку в таблице и добавьте ячейку с изображением.
Aspose.Pdf.Row row2 = tab1.Rows.Add();
row2.BackgroundColor = Color.White;
Aspose.Pdf.Cell cell2 = row2.Cells.Add();
Aspose.Pdf.Image img = new Aspose.Pdf.Image();
img.File = dataDir + "aspose-logo.jpg";
img. FixWidth = 60;
cell2.Paragraphs.Add(img);
row2.Cells.Add("The logo is beautiful!");
row2.Cells[1].DefaultCellTextState.Font = FontRepository.FindFont("Helvetica");
row2.Cells[1].VerticalAlignment = Aspose.Pdf.VerticalAlignment.Center;
row2.Cells[1].Alignment = Aspose.Pdf.HorizontalAlignment.Center;
```

## Шаг 6: Сохранение PDF-документа

После добавления таблицы в раздел заголовка мы можем сохранить документ PDF. Вот как это сделать:

```csharp
// Сохраните PDF-файл
pdfDocument.Save(dataDir + "TableInHeaderFooterSection_out.pdf");
```

Обязательно замените «КАТАЛОГ ВАШИХ ДОКУМЕНТОВ» фактическим путем к каталогу, в котором вы хотите сохранить PDF-документ.

### Пример исходного кода для таблицы в разделе «Верхний и нижний колонтитулы» с использованием Aspose.PDF для .NET 
```csharp

// Путь к каталогу документов.
string dataDir = "YOUR DOCUMENT DIRECTORY";

// Создать экземпляр документа, вызвав пустой конструктор
Aspose.Pdf.Document pdfDocument = new Aspose.Pdf.Document();

// Создать страницу в PDF-документе
Aspose.Pdf.Page page = pdfDocument.Pages.Add();

//Создайте раздел заголовка PDF-файла
Aspose.Pdf.HeaderFooter header = new Aspose.Pdf.HeaderFooter();

// Установите нечетный заголовок для файла PDF
page.Header = header;

// Установите верхнее поле для заголовка
header.Margin.Top = 20;

// Создать экземпляр объекта таблицы
Aspose.Pdf.Table tab1 = new Aspose.Pdf.Table();

// Добавить таблицу в подборку абзацев нужного раздела
header.Paragraphs.Add(tab1);

// Установить границу ячейки по умолчанию с помощью объекта BorderInfo
tab1.DefaultCellBorder = new Aspose.Pdf.BorderInfo(Aspose.Pdf.BorderSide.All, 0.1F);

// Установите ширину столбцов таблицы
tab1.ColumnWidths = "60 300";
Aspose.Pdf.Image img = new Aspose.Pdf.Image();
img.File = dataDir + "aspose-logo.jpg";

// Создайте строки в таблице, а затем ячейки в строках.
Aspose.Pdf.Row row1 = tab1.Rows.Add();
row1.Cells.Add("Table in Header Section");
row1.BackgroundColor = Color.Gray;

// Установите значение диапазона строк для первой строки как 2
tab1.Rows[0].Cells[0].ColSpan = 2;
tab1.Rows[0].Cells[0].DefaultCellTextState.ForegroundColor = Color.Cyan;
tab1.Rows[0].Cells[0].DefaultCellTextState.Font = FontRepository.FindFont("Helvetica");

// Создайте строки в таблице, а затем ячейки в строках.
Aspose.Pdf.Row row2 = tab1.Rows.Add();

// Установить цвет фона для строки Row2
row2.BackgroundColor = Color.White;

// Добавьте ячейку, содержащую изображение.
Aspose.Pdf.Cell cell2 = row2.Cells.Add();

// Установите ширину изображения 60.
img.FixWidth = 60;

// Добавьте изображение в ячейку таблицы
cell2.Paragraphs.Add(img);
row2.Cells.Add("Logo is looking fine !");
row2.Cells[1].DefaultCellTextState.Font = FontRepository.FindFont("Helvetica");

// Установить вертикальное выравнивание текста по центру.
row2.Cells[1].VerticalAlignment = Aspose.Pdf.VerticalAlignment.Center;
row2.Cells[1].Alignment = Aspose.Pdf.HorizontalAlignment.Center;

// Сохраните PDF-файл
pdfDocument.Save(dataDir + "TableInHeaderFooterSection_out.pdf");

```

## Заключение

Поздравляем! Вы узнали, как добавить таблицу в раздел верхнего или нижнего колонтитула документа PDF с помощью Aspose.PDF для .NET. Теперь вы можете настраивать верхние и нижние колонтитулы, добавляя таблицы для отображения дополнительной информации в документах PDF.

### Часто задаваемые вопросы по таблице в разделе «Заголовок и нижний колонтитул»

#### В: Какова цель добавления таблицы в верхний или нижний колонтитул PDF-документа?

A: Добавление таблицы в верхний или нижний колонтитул документа PDF позволяет отображать структурированную и организованную информацию, такую как заголовки, подзаголовки, логотипы или любой другой контент, который должен отображаться последовательно на каждой странице документа.

#### В: Каким образом предоставленный исходный код C# позволяет добавить таблицу в верхний или нижний колонтитул PDF-документа?

A: Код демонстрирует процесс создания пустого документа PDF, добавления страницы, настройки раздела заголовка, создания таблицы со строками и ячейками и, наконец, сохранения документа PDF. Результатом является таблица, отображаемая в разделе заголовка документа PDF.

#### В: Могу ли я настроить внешний вид ячеек таблицы, например границы, цвет фона и стиль текста?

A: Да, вы можете настроить внешний вид ячеек таблицы, задав такие свойства, как границы ячеек, цвет фона, стиль текста, шрифт, размер шрифта и многое другое.

#### В: Как таблица добавляется в заголовок PDF-документа?

A: Код добавляет таблицу в коллекцию абзацев раздела заголовка, что гарантирует отображение таблицы в заголовке каждой страницы.

#### В: Могу ли я добавлять в таблицу больше строк и ячеек по мере необходимости?

 A: Конечно, вы можете добавить больше строк и ячеек в таблицу, используя`Rows.Add()` и`Cells.Add()` методы. Это позволяет структурировать содержимое таблицы по желанию.

#### В: Можно ли настроить ширину столбцов таблицы?
 A: Да, вы можете настроить ширину столбцов таблицы с помощью`ColumnWidths` свойство. Это позволяет вам управлять макетом таблицы.

#### В: Как охватить ячейки несколькими столбцами или строками в таблице?
 A: Чтобы охватить ячейки несколькими столбцами, можно использовать`ColSpan`свойство соответствующей ячейки. Аналогично, вы можете использовать`RowSpan` свойство охватывать ячейки в нескольких строках.

#### В: Что произойдет, если я захочу добавить таблицу как в верхний, так и в нижний колонтитулы PDF-документа?

 A: Вы можете использовать аналогичный подход для обоих разделов: верхнего и нижнего колонтитула. Просто создайте`HeaderFooter` экземпляр для нижнего колонтитула, настройте его и добавьте таблицу в его коллекцию абзацев.

#### В: Могу ли я использовать изображения в ячейках таблицы и как это сделать?

 A: Да, вы можете добавлять изображения в ячейки таблицы. Пример кода демонстрирует добавление изображения в ячейку путем создания`Image` объект, задавая путь к его файлу и размеры, а затем добавляя его в абзацы ячейки.

#### В: Как обеспечить единообразное отображение таблицы на всех страницах PDF-документа?

 A: Когда вы добавляете таблицу в раздел верхнего или нижнего колонтитула с помощью`HeaderFooter` Например, Aspose.PDF гарантирует, что таблица будет отображаться одинаково на каждой странице, обеспечивая единообразный макет.