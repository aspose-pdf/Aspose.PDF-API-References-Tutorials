---
title: ลบรูปภาพจากไฟล์ PDF
linktitle: ลบรูปภาพจากไฟล์ PDF
second_title: เอกสารอ้างอิง Aspose.PDF สำหรับ API ของ .NET
description: ลบรูปภาพจากไฟล์ PDF ได้อย่างง่ายดายด้วย Aspose.PDF สำหรับ .NET
type: docs
weight: 110
url: /th/net/programming-with-images/delete-images/
---
คู่มือนี้จะแนะนำคุณทีละขั้นตอนเกี่ยวกับวิธีการลบรูปภาพออกจากไฟล์ PDF โดยใช้ Aspose.PDF สำหรับ .NET ตรวจสอบให้แน่ใจว่าคุณได้ตั้งค่าสภาพแวดล้อมของคุณแล้ว และทำตามขั้นตอนด้านล่าง:

## ขั้นตอนที่ 1: กำหนดไดเรกทอรีเอกสาร

ก่อนเริ่มต้น ตรวจสอบให้แน่ใจว่าคุณได้ตั้งค่าไดเรกทอรีที่ถูกต้องสำหรับเอกสารแล้ว แทนที่`"YOUR DOCUMENT DIRECTORY"` ในโค้ดพร้อมเส้นทางไปยังไดเร็กทอรีที่เอกสาร PDF ของคุณอยู่

```csharp
string dataDir = "YOUR DOCUMENT DIRECTORY";
```

## ขั้นตอนที่ 2: เปิดเอกสาร PDF

 ในขั้นตอนนี้เราจะเปิดเอกสาร PDF โดยใช้`Document` คลาสของ Aspose.PDF ใช้`Document` สร้างและส่งเส้นทางไปยังเอกสาร PDF

```csharp
Document pdfDocument = new Document(dataDir + "DeleteImages.pdf");

```

## ขั้นตอนที่ 3: ลบรูปภาพเฉพาะ

ในขั้นตอนนี้ เราจะลบรูปภาพเฉพาะจากหน้าใดหน้าหนึ่ง ใช้`Delete` วิธีการของทรัพยากรหน้า`Images` วัตถุที่จะลบรูปภาพ ในตัวอย่างด้านล่าง เราจะลบรูปภาพที่มีดัชนี 1 จากหน้าแรก

```csharp
pdfDocument.Pages[1].Resources.Images.Delete(1);
```

## ขั้นตอนที่ 4: บันทึกไฟล์ PDF ที่อัปเดต

 บันทึกไฟล์ PDF ที่อัปเดตโดยใช้`Save` วิธีการของ`pdfDocument` วัตถุ ระบุเส้นทางเอาท์พุตสำหรับไฟล์ PDF

```csharp
dataDir = dataDir + "DeleteImages_out.pdf";
pdfDocument.Save(dataDir);
```

### ตัวอย่างโค้ดต้นฉบับสำหรับการลบรูปภาพโดยใช้ Aspose.PDF สำหรับ .NET 
```csharp
// เส้นทางไปยังไดเร็กทอรีเอกสาร
string dataDir = "YOUR DOCUMENT DIRECTORY";
// เปิดเอกสาร
Document pdfDocument = new Document(dataDir+ "DeleteImages.pdf");
// การลบรูปภาพใดรูปภาพหนึ่ง
pdfDocument.Pages[1].Resources.Images.Delete(1);
dataDir = dataDir + "DeleteImages_out.pdf";
// บันทึกไฟล์ PDF อัพเดต
pdfDocument.Save(dataDir);
Console.WriteLine("\nImages deleted successfully.\nFile saved at " + dataDir); 
```

## บทสรุป

ขอแสดงความยินดี ! คุณได้ลบรูปภาพออกจากไฟล์ PDF โดยใช้ Aspose.PDF สำหรับ .NET สำเร็จแล้ว ไฟล์ PDF ที่อัปเดตจะถูกบันทึกไว้ในไดเร็กทอรีที่ระบุ ตอนนี้คุณสามารถใช้ไฟล์ PDF นี้ได้โดยไม่ต้องลบรูปภาพ

### คำถามที่พบบ่อยสำหรับการลบรูปภาพจากไฟล์ PDF

#### ถาม: จุดประสงค์ของการลบรูปภาพออกจากไฟล์ PDF โดยใช้ Aspose.PDF สำหรับ .NET คืออะไร

ตอบ การลบรูปภาพออกจากไฟล์ PDF จะช่วยให้คุณลบเนื้อหาภาพที่เฉพาะเจาะจงออกจากเอกสารได้ ไม่ว่าจะเพื่อการแก้ไข การปรับปรุง หรือวัตถุประสงค์อื่น ๆ

#### ถาม: Aspose.PDF สำหรับ .NET ช่วยในการลบรูปภาพจากเอกสาร PDF ได้อย่างไร

A: Aspose.PDF สำหรับ .NET ให้กระบวนการทีละขั้นตอนในการเปิดเอกสาร PDF ระบุและลบรูปภาพเฉพาะจากเอกสาร และบันทึกเอกสาร PDF ที่ปรับเปลี่ยนแล้ว

#### ถาม: เหตุใดจึงสำคัญที่ต้องกำหนดไดเรกทอรีเอกสารก่อนเริ่มลบรูปภาพ?

A: การกำหนดไดเรกทอรีเอกสารจะช่วยให้มั่นใจว่าเอกสาร PDF ถูกระบุตำแหน่งอย่างถูกต้องและไฟล์ PDF ที่แก้ไขแล้วจะถูกบันทึกไว้ในเส้นทางเอาต์พุตที่ต้องการ

####  ถาม: ทำอย่างไร`Document` class in Aspose.PDF for .NET help in deleting images from a PDF file?

 ก. การ`Document` คลาสนี้ช่วยให้คุณสามารถเปิดและจัดการเอกสาร PDF ได้ ในกรณีนี้ คลาสนี้จะใช้เพื่อโหลดไฟล์ PDF ที่จะลบรูปภาพ

#### ถาม: ฉันจะเลือกภาพที่ต้องการลบจากเอกสาร PDF ได้อย่างไร

 A: คุณสามารถใช้`Delete` วิธีการของ`Images` วัตถุภายใน`Resources` ของหน้าใดหน้าหนึ่งเพื่อลบรูปภาพเฉพาะตามดัชนีของหน้านั้น

#### ถาม: ฉันสามารถลบรูปภาพจากหน้าใดๆ ในเอกสาร PDF ได้หรือไม่

A: ใช่ คุณสามารถลบรูปภาพจากหน้าใดๆ ในเอกสาร PDF ได้โดยระบุดัชนีหน้าที่ต้องการและดัชนีของรูปภาพที่ต้องการลบ

#### ถาม: สามารถลบรูปภาพหลาย ๆ รูปจากหน้าต่าง ๆ ในขั้นตอนเดียวได้ไหม?

 A: ใช่ คุณสามารถใช้`Delete` วิธีการหลายหน้าเพื่อลบรูปภาพจากหน้าต่างๆ ในกระบวนการเดียวกัน

#### ถาม: จะเกิดอะไรขึ้นกับเค้าโครงและการจัดรูปแบบของเอกสาร PDF หลังจากลบรูปภาพแล้ว?

ตอบ การลบรูปภาพอาจส่งผลต่อเค้าโครงและการจัดรูปแบบของเอกสาร PDF โดยเฉพาะหากรูปภาพที่ถูกลบไปเป็นส่วนหนึ่งของเค้าโครงเนื้อหา