---
title: ลบข้อความทั้งหมดในไฟล์ PDF
linktitle: ลบข้อความทั้งหมดในไฟล์ PDF
second_title: เอกสารอ้างอิง Aspose.PDF สำหรับ API ของ .NET
description: เรียนรู้วิธีลบข้อความทั้งหมดในไฟล์ PDF โดยใช้ Aspose.PDF สำหรับ .NET
type: docs
weight: 280
url: /th/net/programming-with-text/remove-all-text/
---
ในบทช่วยสอนนี้ เราจะอธิบายวิธีลบข้อความทั้งหมดในไฟล์ PDF โดยใช้ไลบรารี Aspose.PDF สำหรับ .NET เราจะอธิบายกระบวนการทีละขั้นตอนในการเปิด PDF เลือกและลบข้อความจากแต่ละหน้า และบันทึก PDF ที่แก้ไขแล้วโดยใช้โค้ดต้นฉบับ C# ที่ให้มา

## ความต้องการ

ก่อนที่คุณจะเริ่มต้น ให้แน่ใจว่าคุณมีสิ่งต่อไปนี้:

- ติดตั้งไลบรารี Aspose.PDF สำหรับ .NET แล้ว
- ความเข้าใจพื้นฐานเกี่ยวกับการเขียนโปรแกรม C#

## ขั้นตอนที่ 1: ตั้งค่าไดเรกทอรีเอกสาร

 ขั้นแรก คุณต้องตั้งค่าเส้นทางไปยังไดเร็กทอรีที่ไฟล์ PDF ของคุณตั้งอยู่ แทนที่`"YOUR DOCUMENT DIRECTORY"` ใน`dataDir` ตัวแปรที่มีเส้นทางไปยังไฟล์ PDF ของคุณ

```csharp
string dataDir = "YOUR DOCUMENT DIRECTORY";
```

## ขั้นตอนที่ 2: เปิดเอกสาร PDF

 ต่อไปเราเปิดเอกสาร PDF โดยใช้`Document` คลาสจากไลบรารี Aspose.PDF

```csharp
Document pdfDocument = new Document(dataDir + "RemoveAllText.pdf");
```

## ขั้นตอนที่ 3: ลบข้อความออกจากแต่ละหน้า

 เราวนซ้ำผ่านหน้าทั้งหมดของเอกสาร PDF และใช้`OperatorSelector` เพื่อเลือกข้อความทั้งหมดในแต่ละหน้า จากนั้นลบข้อความที่เลือก

```csharp
for (int i = 1; i <= pdfDocument.Pages.Count; i++)
{
     Page page = pdfDocument.Pages[i];
     OperatorSelector operatorSelector = new OperatorSelector(new Aspose.Pdf.Operators.TextShowOperator());
     page.Contents.Accept(operatorSelector);
     page.Contents.Delete(operatorSelector.Selected);
}
```

## ขั้นตอนที่ 4: บันทึก PDF ที่แก้ไขแล้ว

ในที่สุด เราบันทึกเอกสาร PDF ที่แก้ไขแล้วไปยังไฟล์เอาท์พุตที่ระบุ

```csharp
pdfDocument.Save(dataDir + "RemoveAllText_out.pdf", Aspose.Pdf.SaveFormat.Pdf);
```

### ตัวอย่างโค้ดต้นฉบับสำหรับการลบข้อความทั้งหมดโดยใช้ Aspose.PDF สำหรับ .NET 
```csharp
// เส้นทางไปยังไดเร็กทอรีเอกสาร
string dataDir = "YOUR DOCUMENT DIRECTORY";
// เปิดเอกสาร
Document pdfDocument = new Document(dataDir + "RemoveAllText.pdf");
// วนซ้ำผ่านทุกหน้าของเอกสาร PDF
for (int i = 1; i <= pdfDocument.Pages.Count; i++)
{
	Page page = pdfDocument.Pages[i];
	OperatorSelector operatorSelector = new OperatorSelector(new Aspose.Pdf.Operators.TextShowOperator());
	// เลือกข้อความทั้งหมดในหน้า
	page.Contents.Accept(operatorSelector);
	// ลบข้อความทั้งหมด
	page.Contents.Delete(operatorSelector.Selected);
}
// บันทึกเอกสาร
pdfDocument.Save(dataDir + "RemoveAllText_out.pdf", Aspose.Pdf.SaveFormat.Pdf);
```

## บทสรุป

ในบทช่วยสอนนี้ คุณจะได้เรียนรู้วิธีลบข้อความทั้งหมดออกจากเอกสาร PDF โดยใช้ไลบรารี Aspose.PDF สำหรับ .NET โดยปฏิบัติตามคำแนะนำทีละขั้นตอนและดำเนินการตามโค้ด C# ที่ให้มา คุณจะสามารถเปิด PDF เลือกและลบข้อความจากแต่ละหน้า และบันทึก PDF ที่แก้ไขแล้วได้

### คำถามที่พบบ่อย

#### ถาม: จุดประสงค์ของบทช่วยสอน "ลบข้อความทั้งหมดในไฟล์ PDF" คืออะไร

A: บทช่วยสอน "ลบข้อความทั้งหมดในไฟล์ PDF" มีวัตถุประสงค์เพื่อสาธิตวิธีใช้ไลบรารี Aspose.PDF สำหรับ .NET เพื่อลบข้อความทั้งหมดออกจากเอกสาร PDF บทช่วยสอนนี้ให้คำแนะนำทีละขั้นตอนและโค้ดต้นฉบับ C# เพื่อช่วยคุณเปิดเอกสาร PDF เลือกและลบข้อความจากแต่ละหน้า และบันทึก PDF ที่แก้ไขแล้ว

#### ถาม: เหตุใดฉันจึงต้องการลบข้อความทั้งหมดออกจากเอกสาร PDF

A: มีสถานการณ์ต่างๆ มากมายที่การลบข้อความทั้งหมดออกจากเอกสาร PDF อาจเป็นประโยชน์ได้ ตัวอย่างเช่น คุณอาจต้องการสร้างเอกสารเวอร์ชันที่แก้ไขแล้วโดยการลบข้อมูลที่ละเอียดอ่อน หรือคุณอาจต้องการสร้างการแสดงภาพของเอกสารโดยไม่มีเนื้อหาข้อความ

#### ถาม: ฉันจะตั้งค่าไดเรกทอรีเอกสารได้อย่างไร?

ก: การตั้งค่าไดเรกทอรีเอกสาร:

1.  แทนที่`"YOUR DOCUMENT DIRECTORY"` ใน`dataDir` ตัวแปรที่มีเส้นทางไปยังไดเร็กทอรีที่ไฟล์ PDF ของคุณอยู่

#### ถาม: ฉันจะลบข้อความจากแต่ละหน้าของเอกสาร PDF ได้อย่างไร

 A: บทช่วยสอนนี้จะแนะนำคุณตลอดกระบวนการในการวนซ้ำผ่านหน้าต่างๆ ของเอกสาร PDF โดยเลือกข้อความทั้งหมดในแต่ละหน้าโดยใช้`OperatorSelector`แล้วลบข้อความที่เลือก

#### ถาม: ฉันสามารถลบข้อความจากหน้าเฉพาะเจาะจงได้ไหม?

A: ใช่ คุณสามารถปรับเปลี่ยนลูปเพื่อลบข้อความจากหน้าเฉพาะได้โดยระบุหมายเลขหน้าที่คุณต้องการประมวลผล ตัวอย่างที่ให้ไว้ในบทช่วยสอนจะสาธิตวิธีการวนซ้ำผ่านหน้าทั้งหมด แต่คุณสามารถปรับเปลี่ยนให้ตรงตามความต้องการของคุณได้

#### ถาม: ฉันจะบันทึกเอกสาร PDF ที่แก้ไขแล้วได้อย่างไร

 A: หลังจากลบข้อความออกจากแต่ละหน้าแล้ว คุณสามารถบันทึกเอกสาร PDF ที่แก้ไขแล้วโดยใช้`Save` วิธีการของ`Document`คลาส ระบุเส้นทางไฟล์เอาท์พุตที่ต้องการและระบุรูปแบบการบันทึกที่ต้องการเป็นอาร์กิวเมนต์ของ`Save` วิธี.

#### ถาม: ผลลัพธ์ที่คาดหวังจากบทช่วยสอนนี้คืออะไร?

A: เมื่อทำตามบทช่วยสอนและรันโค้ด C# ที่ให้มา คุณจะสร้างเอกสาร PDF ที่ปรับเปลี่ยนแล้ว โดยที่ข้อความทั้งหมดในแต่ละหน้าจะถูกลบออกไป

#### ถาม: ฉันสามารถใช้ตัวดำเนินการอื่นเพื่อลบเนื้อหาประเภทอื่นได้หรือไม่

A: ใช่ คุณสามารถใช้ตัวดำเนินการที่แตกต่างกันเพื่อกำหนดเป้าหมายและลบเนื้อหาประเภทต่างๆ ออกจากเอกสาร PDF เช่น รูปภาพหรือองค์ประกอบกราฟิก ตัวอย่างที่ให้ไว้ในบทช่วยสอนเน้นไปที่การลบข้อความโดยเฉพาะ

#### ถาม: จำเป็นต้องมีใบอนุญาต Aspose ที่ถูกต้องสำหรับบทช่วยสอนนี้หรือไม่

A: ใช่ ต้องมีใบอนุญาต Aspose ที่ถูกต้องเพื่อให้บทช่วยสอนนี้ทำงานได้อย่างถูกต้อง คุณสามารถซื้อใบอนุญาตเต็มรูปแบบหรือรับใบอนุญาตชั่วคราว 30 วันได้จากเว็บไซต์ Aspose